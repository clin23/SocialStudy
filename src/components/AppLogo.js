import React from "react";
import { BookOpen } from "lucide-react";

const AppLogo = () => {
  return (
    <div className="app-logo">
      <BookOpen size={24} />
      <span>SocialStudy</span>
    </div>
  );
};

export default AppLogo;
